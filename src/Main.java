package src;
import java.time.LocalDate;
import java.util.ArrayList;

public class Main {
    static ArrayList<Wiki> wikis = new ArrayList<>();

    public static void main(String[] args) {//main method: creates and reads every file from deck directory and runs everytime to ensure deck directory is always there. Also runs ui.
    }
}